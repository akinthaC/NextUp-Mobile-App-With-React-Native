export interface Task {
    id?: string; // ? meken kiyenne id ek optional data ekk create weddi firebse eke id ek assign karan nisa
    title: string;
    description: string;
    userId?: string;
    
}